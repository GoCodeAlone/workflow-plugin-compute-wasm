# workflow-plugin-compute-wasm

Public WASM and browser runtime adapter plugin for Workflow Compute.

This repository owns runtime adapter contracts and invocation helpers for:

- `wasm-component`
- `product-capture-browser`

`workflow-compute` remains responsible for task admission, leases, tenant
authorization, artifact accounting, and proof/reward mutation.
